<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/full_backup_content"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/clear_text_traffic_permitted_network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning"
        tools:replace="android:allowBackup"
        tools:targetApi="s">

        <activity
            android:name=".activities.SplashV2"
            android:exported="true"
            android:theme="@style/AppTheme.NoActionBar">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity
            android:name=".activities.Splash"
            android:label="Wallet Ledger"
            android:theme="@style/AppTheme.NoActionBar">

            <!--             <intent-filter>
                            <action android:name="android.intent.action.MAIN" />
                            <category android:name="android.intent.category.LAUNCHER" />
                        </intent-filter> -->

        </activity>

        <activity android:name="ndk.utils_android16.activities.LoginBundleActivity" />

        <activity
            android:name=".activities.Insert_Transaction"
            android:label="Wallet Ledger" />

        <activity
            android:name=".activities.ClickablePassBookBundle"
            android:label="Pass Book"
            android:screenOrientation="fullSensor" />

        <activity android:name=".activities.InsertTransactionV2" />
        <activity android:name=".activities.InsertTransactionV2Via" />
        <activity android:name=".activities.ListAccounts" />
        <activity android:name=".activities.Insert_Account" />
        <activity android:name=".activities.InsertTransactionV2Quick" />
        <activity android:name=".activities.Edit_Transaction_v2" />
        <activity android:name=".activities.InsertTransactionV2TwoWay" />
        <activity android:name=".activities.InsertTo2SplitTransactionV2Activity" />

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />

    </application>

</manifest>
